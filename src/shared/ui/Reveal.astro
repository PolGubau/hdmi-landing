---
type Props = {
  delay?: number;
  as?: keyof HTMLElementTagNameMap;
  class?: string;
};

const { delay = 0, as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag
  data-reveal
  style={`--reveal-delay:${delay}ms`}
  class={`reveal reveal-hidden ${className}`}
>
  <slot />
</Tag>
