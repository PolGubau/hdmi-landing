---
import { getLangFromUrl, useTranslations } from "~/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Placeholder projects - you can replace with actual content collection later
const projects = [
  {
    title: "Novahair",
    description: "Gestión",
    image: "/placeholder-project-1.jpg",
    tags: ["React", "Automatización"],
  },
  {
    title: "Dashboard Empresarial",
    description: "Panel de control con analíticas en tiempo real",
    image: "/placeholder-project-2.jpg",
    tags: ["Dashboard", "Analytics", "API"],
  },
  {
    title: "App de Reservas",
    description: "Sistema de reservas automatizado con WhatsApp",
    image: "/placeholder-project-3.jpg",
    tags: ["Automation", "WhatsApp", "Calendar"],
  },
];
---

<section
  id="portfolio"
  class="px-6 lg:px-8 py-32 sm:py-40 w-full mx-auto max-w-7xl"
>
  <header
    class="intersect-once intersect:motion-preset-blur-down motion-delay-200 first_hidden mb-12"
  >
    <h2>{t("portfolio.title")}</h2>
  </header>

  <ul class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
    {
      projects.map((project) => (
        <article class="group flex flex-col gap-8 rounded-2xl transition-all duration-300">
          <div class="aspect-video rounded-md bg-gradient-to-br from-primary/20 to-primary/5 dark:from-primary/30 dark:to-primary/10 flex items-center justify-center overflow-hidden transition-all">
            <div class="group-hover:scale-110 transition-[scale]">
              <span class="text-4xl opacity-50 transition-all">🚀</span>
            </div>
          </div>
          <div class="pr-4 text-balance">
            <h3 class="project-title">{project.title}</h3>
            <p class="text-muted-foreground text-sm mb-4">
              {project.description}
            </p>
          </div>
        </article>
      ))
    }
  </ul>
</section>
