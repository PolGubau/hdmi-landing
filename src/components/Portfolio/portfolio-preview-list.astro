---
import { getLangFromUrl, useTranslations } from "~/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Placeholder projects - you can replace with actual content collection later
const projects = [
  {
    title: "E-commerce Moderno",
    description: "Tienda online completa con pasarela de pago integrada",
    image: "/placeholder-project-1.jpg",
    tags: ["E-commerce", "React", "Stripe"],
  },
  {
    title: "Dashboard Empresarial",
    description: "Panel de control con analíticas en tiempo real",
    image: "/placeholder-project-2.jpg",
    tags: ["Dashboard", "Analytics", "API"],
  },
  {
    title: "App de Reservas",
    description: "Sistema de reservas automatizado con WhatsApp",
    image: "/placeholder-project-3.jpg",
    tags: ["Automation", "WhatsApp", "Calendar"],
  },
];
---

<section
  id="portfolio"
  class="px-6 py-32 sm:py-40 lg:px-8 bg-muted/30 dark:bg-muted/10"
>
  <div class="mx-auto max-w-7xl flex flex-col gap-16">
    <header class="flex gap-2 flex-col">
      <h2>{t("portfolio.title")}</h2>
    </header>

    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
      {
        projects.map((project) => (
          <article
            class={`group flex flex-col gap-8 rounded-2xl transition-all duration-300`}
          >
            <div class="aspect-video rounded-md bg-gradient-to-br from-primary/20 to-primary/5 dark:from-primary/30 dark:to-primary/10 flex items-center justify-center">
              <span class="text-4xl opacity-50">🚀</span>
            </div>
            <div class="pr-4 text-balance">
              <h3 class="project-title">{project.title}</h3>
              <p class="text-muted-foreground text-sm mb-4">
                {project.description}
              </p>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</section>
